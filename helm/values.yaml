# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: viktordevopscourse
  tag: "v1.0.4-4516915"
  arch: arm64

secret:
  name: TELE_TOKEN
  tokenKey: token
  tokenName: kbot